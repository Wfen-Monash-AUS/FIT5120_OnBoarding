import { createApp } from 'vue'
import App from './App.vue'

// Import only global styles that are absolutely necessary, or none if we handle it in App.vue
// import './assets/main.css'

createApp(App).mount('#app')
